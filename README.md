# Cloudflare Project

This project demonstrates the implementation of a secure web endpoint using:

- AWS EC2
- Nginx reverse proxy
- Cloudflare DNS
- Cloudflare Workers
- Cloudflare R2 storage
- Cloudflare Zero Trust authentication

Endpoint:
/secure

Response:
${EMAIL} authenticated at ${TIMESTAMP} from ${COUNTRY}
